services:
  komodo-periphery:
    image: ghcr.io/moghtech/komodo-periphery:2
    container_name: komodo-periphery
    restart: unless-stopped
    network_mode: host
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - periphery-keys:/config/keys
    environment:
      - KOMODO_SECRET=MCowBQYDK2VuAyEAQzm6PFQP/YhNdIoHYLNz@P@or/&x/Xw
      - PERIPHERY_CORE_ADDRESS=http://10.0.0.117:9120
      - PERIPHERY_CONNECT_AS=komodo-node1
      - PERIPHERY_PORT=9121
      - PERIPHERY_SSL_ENABLED=false
      - TZ=Europe/Oslo

volumes:
  periphery-keys:
